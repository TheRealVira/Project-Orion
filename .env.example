# ========================================
# PROJECT ORION - ENVIRONMENT CONFIGURATION
# ========================================
# Copy this file to .env and configure according to your needs
# Most settings are optional - the app works with defaults out of the box

# ========================================
# Application Settings
# ========================================
# Base URL for the application (used in OAuth redirects and email links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ========================================
# Database Configuration
# ========================================
# SQLite database path (default: ./orion.db)
# DATABASE_PATH=./orion.db

# ========================================
# Authentication Settings
# ========================================
# NextAuth URL (should match your application URL)
NEXT_AUTH_URL=http://localhost:3000

# Session duration (default: 7 days)
# SESSION_MAX_AGE=604800

# ========================================
# Default Admin Account
# ========================================
# Configure the default admin user that is created on first startup
# Change these values before first run, or change the password after first login!
DEFAULT_ADMIN_EMAIL=admin@orion.local
DEFAULT_ADMIN_NAME=Administrator
DEFAULT_ADMIN_PASSWORD=admin123

# ========================================
# LDAP Configuration (Optional)
# ========================================
# Enable LDAP authentication by uncommenting these settings
# LDAP_URL=ldap://ldap.example.com:389
# LDAP_BIND_DN=cn=admin,dc=example,dc=com
# LDAP_BIND_PASSWORD=your_bind_password
# LDAP_SEARCH_BASE=ou=users,dc=example,dc=com
# LDAP_SEARCH_FILTER=(uid={{username}})
# LDAP_USERNAME_ATTR=uid
# LDAP_EMAIL_ATTR=mail
# LDAP_NAME_ATTR=cn

# LDAP Examples:
# - Active Directory: ldap://ad.company.com:389
#   LDAP_SEARCH_BASE=DC=company,DC=com
#   LDAP_SEARCH_FILTER=(sAMAccountName={{username}})
# - OpenLDAP: ldap://openldap.company.com:389
#   LDAP_SEARCH_BASE=ou=People,dc=company,dc=com
#   LDAP_SEARCH_FILTER=(uid={{username}})

# ========================================
# OAuth - GitHub (Optional)
# ========================================
# Setup: https://github.com/settings/developers
# Create a new OAuth App with callback URL: http://localhost:3000/api/auth/callback/github
# OAUTH_GITHUB_CLIENT_ID=your_github_client_id
# OAUTH_GITHUB_CLIENT_SECRET=your_github_client_secret

# ========================================
# OAuth - Google (Optional)
# ========================================
# Setup: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID with callback URL: http://localhost:3000/api/auth/callback/google
# OAUTH_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
# OAUTH_GOOGLE_CLIENT_SECRET=your_google_client_secret

# ========================================
# OAuth - Microsoft Azure AD (Optional)
# ========================================
# Setup: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
# Register an app with redirect URI: http://localhost:3000/api/auth/callback/microsoft
# OAUTH_MICROSOFT_CLIENT_ID=your_microsoft_client_id
# OAUTH_MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret
# OAUTH_MICROSOFT_TENANT=common
# Note: Use 'common' for multi-tenant, or your specific tenant ID

# ========================================
# OAuth - GitLab (Optional)
# ========================================
# Setup: https://gitlab.com/-/profile/applications
# Create application with callback URL: http://localhost:3000/api/auth/callback/gitlab
# Required scopes: read_user
# OAUTH_GITLAB_CLIENT_ID=your_gitlab_client_id
# OAUTH_GITLAB_CLIENT_SECRET=your_gitlab_client_secret
# OAUTH_GITLAB_URL=https://gitlab.com
# Note: For self-hosted GitLab, change OAUTH_GITLAB_URL to your instance URL

# ========================================
# OAuth - Custom OIDC Provider (Optional)
# ========================================
# Configure any OpenID Connect compatible provider
# OAUTH_CUSTOM_CLIENT_ID=your_custom_client_id
# OAUTH_CUSTOM_CLIENT_SECRET=your_custom_client_secret
# OAUTH_CUSTOM_ISSUER=https://your-oidc-provider.com
# OAUTH_CUSTOM_AUTHORIZATION_URL=https://your-oidc-provider.com/oauth/authorize
# OAUTH_CUSTOM_TOKEN_URL=https://your-oidc-provider.com/oauth/token
# OAUTH_CUSTOM_USER_INFO_URL=https://your-oidc-provider.com/oauth/userinfo
# OAUTH_CUSTOM_NAME=Custom Provider
# Required scopes: openid profile email

# ========================================
# Email Configuration (Optional)
# ========================================
# Configure SMTP to send incident notifications
# If not configured, notifications will be logged to console only

# SMTP Server Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# From Address
FROM_EMAIL=orion@example.com
FROM_NAME=Project Orion

# Gmail Setup Instructions:
# 1. Go to https://myaccount.google.com/apppasswords
# 2. Generate an "App Password" for "Mail"
# 3. Use that password as SMTP_PASS (not your regular password)

# Other SMTP Providers:
# - Outlook: smtp.office365.com:587
# - Yahoo: smtp.mail.yahoo.com:587
# - SendGrid: smtp.sendgrid.net:587
# - Mailgun: smtp.mailgun.org:587

# ========================================
# SMS Configuration (Optional - Disabled by Default)
# ========================================
# Configure SMS notifications for critical incidents
# SMS is DISABLED by default - set ENABLE_SMS_NOTIFICATIONS=true to enable

# Enable SMS Notifications (default: false)
# ENABLE_SMS_NOTIFICATIONS=true

# Twilio Settings (Recommended)
# TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_AUTH_TOKEN=your_auth_token
# TWILIO_FROM_NUMBER=+1234567890

# SMS Provider (default: twilio)
# Supported: twilio, vonage, aws-sns
# SMS_PROVIDER=twilio

# Twilio Setup Instructions:
# 1. Sign up at https://www.twilio.com/
# 2. Go to Console > Account Info
# 3. Copy your Account SID and Auth Token
# 4. Get a phone number from Console > Phone Numbers
# 5. Use that number as TWILIO_FROM_NUMBER (with + and country code)

# Note: Twilio trial accounts can only send to verified phone numbers
# For production, upgrade your account to send to any number

# ========================================
# Webhook Configuration (Optional)
# ========================================
# Webhook secret for verifying incoming alert webhooks
# Generate a random string: openssl rand -hex 32
# WEBHOOK_SECRET=your_webhook_secret_here

# ========================================
# Feature Flags (Optional)
# ========================================
# Enable/disable features (default: most enabled, SMS disabled)
# ENABLE_WEBHOOKS=true
# ENABLE_EMAIL_NOTIFICATIONS=true
# ENABLE_SMS_NOTIFICATIONS=false
# ENABLE_OAUTH=true
# ENABLE_LDAP=true

# ========================================
# Logging Configuration (Optional)
# ========================================
# Log level: error, warn, info, debug (default: info)
# LOG_LEVEL=info

# ========================================
# Production Settings
# ========================================
# For production deployment, also configure:
# - NODE_ENV=production
# - Proper HTTPS URLs for NEXT_PUBLIC_APP_URL and NEXT_AUTH_URL
# - Strong SESSION_SECRET (generate with: openssl rand -base64 32)
# - Real OAuth credentials with production callback URLs
# - Secure SMTP credentials
# - Database backups enabled
# - Rate limiting configured (see src/middleware.ts)

# ========================================
# Development Settings
# ========================================
# Uncomment to enable development features
# NEXT_PUBLIC_DEV_MODE=true
# SKIP_EMAIL_VERIFICATION=true
